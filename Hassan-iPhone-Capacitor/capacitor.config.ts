import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hassan.accounting',
  appName: 'برنامج حسان المحاسبي',
  webDir: 'www'
};

export default config;
